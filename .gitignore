# Hugo 构建产物（由 Netlify 自动生成，不需要提交）
public/
resources/
.hugo_build.lock

# 依赖
node_modules/

# 编辑器 / 系统文件
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 本地环境变量
.env
.env.local

# 这个文件需要 GitHub 的 workflow 权限才能通过 git 推送，
# 所以由网页界面创建，本地不追踪。
/.github/workflows/news.yml
