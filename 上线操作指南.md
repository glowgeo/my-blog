# 上线操作指南（手把手版）

从零到网站上线，全程不需要信用卡、不需要服务器、不需要备案。

预计耗时 **25–40 分钟**，其中大部分时间是在等邮件和等构建。

---

## 现在的状态

代码已经全部就绪，本地构建通过。你只差三件事：

```
① 把代码推到 GitHub          ← 第 3 步
② 在 Netlify 注册并部署      ← 第 4、5 步
③ 开通写作后台               ← 第 6 步
```

做完这三步，你的博客就永久在线了（跟你电脑开不开机没关系）。

---

## 目录

- [第 0 步：先在本地看一眼](#第-0-步先在本地看一眼)
- [第 1 步：注册 GitHub 账号](#第-1-步注册-github-账号)
- [第 2 步：把占位信息改成你自己的](#第-2-步把占位信息改成你自己的)
- [第 3 步：在 GitHub 建仓库并推送代码](#第-3-步在-github-建仓库并推送代码)
- [第 4 步：注册 Netlify 并部署](#第-4-步注册-netlify-并部署)
- [第 5 步：改掉那个随机网址](#第-5-步改掉那个随机网址)
- [第 6 步：开通写作后台](#第-6-步开通写作后台)
- [第 7 步：绑定自己的域名（可选）](#第-7-步绑定自己的域名可选)
- [第 8 步：开启评论功能（可选）](#第-8-步开启评论功能可选)
- [日常写作流程](#日常写作流程)
- [已经为你做好的优化](#已经为你做好的优化)
- [常见问题排查](#常见问题排查)
- [备份](#备份)
- [总共花了多少钱](#总共花了多少钱)

---

## 第 0 步：先在本地看一眼

Hugo 已经装好了，但在你**新开一个** PowerShell 窗口之前，它可能还不在 PATH 里。

打开一个新的 PowerShell，执行：

```powershell
cd C:\Users\GEO\Desktop\my-blog
hugo server -D
```

浏览器打开 **http://localhost:1313**

你应该能看到完整的博客——首页、文章、归档、搜索、深色模式切换全部可用。

`-D` 表示连草稿一起显示。按 `Ctrl + C` 停止。

> 这一步**只是本地预览**，只有你自己能看见，和后面要上线的网站是两回事。
>
> 如果提示找不到 `hugo` 命令，关掉 PowerShell 重新开一个。还不行就重启电脑。

---

## 第 1 步：注册 GitHub 账号

GitHub 是用来**存你文章的地方**，相当于一个带版本历史的云端硬盘。注册免费，**不需要信用卡**。

如果你已经有 GitHub 账号，跳到第 2 步。

### 1.1 注册

1. 打开 **https://github.com/signup**
2. 填**邮箱** → Continue
   - Gmail、Outlook、QQ 邮箱都行
3. 设**密码** → Continue
   - 至少 8 位，必须包含数字和小写字母。太简单的会被拒绝
4. 填**用户名（Username）**
   - 只能用**字母、数字、连字符**，不能用中文和空格
   - 这会成为你的主页地址 `github.com/用户名`，**也会出现在你博客的默认网址里**
   - 起个好记的，比如 `zhangsan-note`
5. 完成人机验证（通常是拼图）
6. 收邮箱验证码，填入
7. 后面会问你的身份（学生 / 团队 / 随便看看）和付费计划
   - **选 Free（免费版）**
   - 其他的随便选或跳过

注册完成，你会进入 GitHub 首页。

> 💡 **国内访问 GitHub 可能不稳定。** 如果页面加载不出来或者很慢，换个网络环境再试，或者用 [GitHub Desktop](https://desktop.github.com/) 客户端代替网页操作。

---

## 第 2 步：把占位信息改成你自己的

在推送之前先把这些改掉，省得后面还要重新部署。

### 2.1 设置 Git 提交身份

现在仓库里用的是占位身份（`my-blog` / `you@example.com`），需要换成你自己的，否则 GitHub 无法把你的提交算在你名下。

仓库现在只有**一条**提交，作者是占位值（`my-blog` / `you@example.com`）。因为你还没推送到任何地方，直接改掉它就行：

```powershell
cd C:\Users\GEO\Desktop\my-blog

git config user.name "你的名字"
git config user.email "你的GitHub注册邮箱"

# 把这条提交的作者改成你自己
git commit --amend --reset-author --no-edit
```

**把名字和邮箱换成你自己的。** 邮箱要和注册 GitHub 用的一致，这样 GitHub 才会把提交算在你名下（提交记录里会显示你的头像）。

想确认的话，执行：

```powershell
git log --format="%an <%ae>"
```

应该显示你自己的名字和邮箱。

### 2.2 改站点信息

用记事本或 VS Code 打开 `hugo.yaml`，把下面这些改成你自己的：

```yaml
title: "我的博客"                          # ← 站点名字，显示在浏览器标签和左上角
params:
  description: "记录我的思考、笔记与生活"     # ← 站点简介，搜索引擎会用到
  author: "你的名字"                        # ← 作者名
  title: "我的博客"                         # ← 和上面那个保持一致
  socialIcons:
    - name: github
      url: "https://github.com/你的用户名"    # ← 换成你的 GitHub 主页
```

同时改一下 `homeInfoParams`，那是首页的欢迎语：

```yaml
  homeInfoParams:
    Title: "你好，欢迎来坐坐 👋"
    Content: >
      这里是我的个人博客。
      我会在这里记录一些想法、笔记和踩过的坑。
```

最后把 `content/about.md` 里的自我介绍也改掉。

改完保存即可，暂时不用执行任何命令。

---

## 第 3 步：在 GitHub 建仓库并推送代码

### 3.1 建一个空仓库

1. 打开 **https://github.com/new**
2. **Repository name** 填 `my-blog`（或任何你喜欢的名字）
3. **Public / Private 怎么选？**

   | 选项 | 说明 |
   |---|---|
   | **Public** | 任何人都能看到你的文章源码。以后想开评论（Giscus）**必须**用这个 |
   | **Private** | 更私密，但评论功能用不了 |

   **建议选 Public。** 这只是一堆 Markdown 文本，没什么可藏的，而且以后随时能改回来。

4. ⚠️ **下面这三个勾全部不要打：**
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license

   > 因为本地已经有内容了。勾了会导致推送时报错
   > `! [rejected] main -> main (fetch first)`，还得额外处理一次。

5. 点 **Create repository**

创建完你会看到一个空仓库页面，上面有操作提示。我们要用到的是 **"…or push an existing repository from the command line"** 那一段的地址。

### 3.2 推送

回到 PowerShell：

```powershell
cd C:\Users\GEO\Desktop\my-blog

git remote add origin https://github.com/你的用户名/my-blog.git
git push -u origin main
```

**把 `你的用户名` 换成第 1 步注册的用户名。**

> 如果提示 `error: remote origin already exists`，改用这条先覆盖地址：
> ```powershell
> git remote set-url origin https://github.com/你的用户名/my-blog.git
> ```
> 然后再执行 `git push -u origin main`

### 3.3 关于登录凭证

第一次推送时会要求登录，可能有三种情况：

**情况 A：弹出 "Git Credential Manager" 窗口（最常见）**

点 **Sign in with your browser** → 浏览器会打开 GitHub 授权页 → 点 **Authorize** → 完成。

**情况 B：命令行里要你输入用户名和密码**

⚠️ **GitHub 早就不接受账号密码了。** 你需要生成一个 Personal Access Token 当作密码：

1. GitHub 右上角头像 → **Settings**
2. 左侧栏拉到最下面 → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. **Note** 随便填，比如 `blog`
6. **Expiration** 选 `90 days` 或 `No expiration`
7. **勾选 `repo`** 这一整个大项（必须）
8. 拉到最下面 → **Generate token**
9. 复制那串 `ghp_xxxxxxxx`（**只显示这一次，务必先存好**）

回到 PowerShell 重新 `git push`：

- `Username`: 填你的 GitHub 用户名
- `Password`: **粘贴刚才那串 token**（粘贴时屏幕不显示字符，是正常的，直接回车）

**情况 C：报错 `Failed to connect to github.com`**

网络问题。换网络环境，或者用 GitHub Desktop 客户端推送。

### 3.4 确认成功

推送成功会看到类似输出：

```
To https://github.com/你的用户名/my-blog.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.
```

**刷新 GitHub 仓库页面**，应该能看到 `hugo.yaml`、`content`、`themes` 等文件。

到这一步，你的文章已经在云端了，**即使你现在把电脑砸了也不会丢**。

---

## 第 4 步：注册 Netlify 并部署

Netlify 是真正让网站跑起来的地方。它会把你的代码构建成静态网站，然后分发到全球 CDN 上。

### 4.1 注册（用 GitHub 一键登录）

1. 打开 **https://app.netlify.com/signup**
2. **点那个 "GitHub" 按钮**（不要填邮箱表单）
3. 弹出 GitHub 授权页 → 点 **Authorize Netlify**
4. 可能会问你是个人还是团队，随便填一个
5. 进入 Netlify 后台

✅ **全程不需要信用卡。**

### 4.2 部署你的网站

1. 点 **Add new site** → **Import an existing project**
2. 选 **GitHub**
3. 第一次会要求安装 Netlify 到你的 GitHub：
   - 选 **Only select repositories**
   - 勾上 `my-blog`
   - 点 **Install** / **Save**
   - （如果注册时已经授权过全部仓库，会直接跳到下一步）
4. 在仓库列表里点选 **my-blog**
5. **构建设置页面**
   - 因为仓库里有 `netlify.toml`，Netlify **会自动读取**，你应该看到：
     ```
     Branch to deploy:   main
     Build command:      hugo --gc --minify -b $URL
     Publish directory:  public
     ```
   - **确认这三个值是对的。** 如果 Netlify 没自动填，就手动照上面填进去
6. 点 **Deploy**（或 **Deploy my-blog**）

### 4.3 等待构建

会进入部署日志页面，滚动能看到 Hugo 的构建输出，最后出现：

```
Total in xxx ms
```

以及状态变成 **Published**。大约 1–2 分钟。

页面顶部会出现一个网址，形如：

```
https://sparkly-cat-8f3a21.netlify.app
```

**点它。你的博客上线了。** 🎉

> 从现在开始，**这台电脑关机、断网、损坏，网站都照常访问**。
> 你甚至可以在手机上访问它。

---

## 第 5 步：改掉那个随机网址

`sparkly-cat-8f3a21.netlify.app` 这种随机名字不好看也不好记。改成你自己的（**免费**）：

1. 在你的站点页面，点 **Site configuration**
2. 找到 **Site details**（或首屏的 **Site information**）
3. 找到 **Site name** → 点 **Change site name**
4. 输入你想要的，比如 `zhangsan-blog`
   - 只能用小写字母、数字和连字符
   - 全球唯一，被占用会提示换一个
5. 保存

新网址就是：

```
https://zhangsan-blog.netlify.app
```

**这就是你的博客地址，永久免费。** 以后想换成自己的域名（第 7 步），随时可以加，不用重新部署。

> ⚠️ 改了站点名之后，旧的随机网址会失效。如果你已经在哪分享过，记得更新。

---

## 第 6 步：开通写作后台

现在网站能看了，但 `/admin/` 还登录不了，因为还没开认证服务。

### 6.1 开启 Identity

1. 在 Netlify 站点页面，找 **Integrations** → **Identity**
   - （旧版界面可能在 **Site configuration → Identity**）
2. 点 **Enable Identity**
3. 进入 **Configuration and usage** 设置页
4. **Registration preferences** → 选 **Invite only**

   > ⚠️ **千万不要选 Open。** 选了之后，任何人只要知道你的网址就能注册账号，
   > 而注册后就能通过 Git Gateway 修改你的文章。

5. **External providers**（可选）→ 勾上 **GitHub**
   - 勾了以后可以用 GitHub 一键登录，不用记密码

### 6.2 开启 Git Gateway

在同一个设置页面向下滚动，找到 **Services** → **Git Gateway**，点 **Enable**。

**Roles 字段留空**即可。

> Git Gateway 的作用：让你的后台账号有权限往 GitHub 写文件，而不用把 GitHub 账号密码给 Netlify。

### 6.3 邀请自己

1. 回到 **Identity** 页面
2. 点 **Invite users**
3. 填入**你自己的邮箱**，发送邀请
4. 去邮箱收信（**记得看垃圾邮件箱**）
5. 点邮件里的链接
6. 会打开你的网站，并弹出一个窗口让你**设置密码**
7. 设好后自动跳转到后台

> **如果点邮件链接没反应**：不用管，直接手动打开 `https://你的网址/admin/`，
> 用刚设置的邮箱密码登录即可。

### 6.4 登录并写第一篇文章

打开：

```
https://你的网址/admin/
```

点 **Login with Netlify Identity**，输入邮箱密码。

进去后你会看到左侧有「文章」和「独立页面」两个集合：

1. 点 **New 文章**
2. 填标题、写正文、可以拖图片进去
3. 右上角点 **Publish**
4. 等 30 秒左右，Netlify 会自动重新构建
5. 刷新你的网站，文章就出现了

**把示例文章删掉**：点进 `hello-world`，左下角菜单里选 **Delete entry**。

---

## 第 7 步：绑定自己的域名（可选）

不绑定完全没关系，`xxx.netlify.app` 可以一直免费用。但有自己的域名会更专业，而且以后换托管商也不用通知读者。

### 7.1 买域名

**这些地方可以用支付宝买，不需要信用卡：**

| 平台 | 说明 |
|---|---|
| 阿里云 / 腾讯云 | 国内主流，中文界面，支付宝付款 |
| [NameSilo](https://www.namesilo.com/) | 国外注册商，支持支付宝，价格透明，续费不涨价 |

**选什么后缀：**

| 后缀 | 首年 | 续费 | 建议 |
|---|---|---|---|
| **`.com`** | ¥60–80 | 约 ¥70–90/年 | ⭐ **首选**，最通用，价格稳定 |
| `.online` / `.top` / `.xyz` | ¥8–30 | ¥80–130 | ⚠️ 首年便宜是引流价，第二年涨十几倍 |
| `.cn` | ¥20–40 | ¥30–40 | 实名要求更严，新手容易踩坑 |

> **下单前一定要把鼠标移到价格说明上，确认「续费价」是多少。**
> 别只看首年那个 ¥8。

买域名需要**实名认证**（传身份证），审核 1–3 天。这**不是备案**，只是实名。

### 7.2 在 Netlify 绑定

1. Netlify：**Domain management → Domains → Add a domain**
2. 输入你的域名 → **Verify** → **Add domain**
3. Netlify 会给出两种配置方式，二选一：
   - **改 NS（推荐）**：去域名商后台，把域名的 DNS 服务器改成 Netlify 给的两个地址。之后所有解析都在 Netlify 管，最省事
   - **加 CNAME**：在域名商后台加一条 CNAME 记录，指向 `你的站点名.netlify.app`
4. 等 DNS 生效（几分钟到 24 小时）
5. Netlify 会**自动申请并续期 HTTPS 证书**，不需要你做任何事

### 7.3 常见疑问

**需要备案吗？**
**不需要。** 备案只在国内服务器上才要求。你的网站跑在 Netlify（国外）。

**国内访问速度怎么样？**
能开，但不算快。想更快的话，可以把同一份代码再部署一份到 **Cloudflare Pages**（同样免费、同样从 GitHub 自动构建），然后绑同一个域名。代码一行都不用改。

---

## 第 8 步：开启评论功能（可选）

用 **Giscus**，基于 GitHub Discussions，完全免费、没有广告、不需要服务器。

**前提**：你的博客仓库必须是 **Public**。

1. **开启 Discussions**
   仓库页面 → **Settings** → **General** → 往下找到 **Features** → 勾选 **Discussions**

2. **安装 Giscus App**
   打开 https://github.com/apps/giscus → **Install** → 选你的 `my-blog` 仓库

3. **获取配置值**
   打开 https://giscus.app/zh-CN ，在页面上：
   - Repository 填 `你的用户名/my-blog`，确认它能识别
   - Discussion Category 选 `Announcements`
   - 页面下方会生成一段配置代码，从中找到 `data-repo-id` 和 `data-category-id`

4. **填进配置**

   打开 `hugo.yaml`，找到 `giscus:` 那一段：

   ```yaml
   params:
     comments: true            # ← 从 false 改成 true
     giscus:
       repo: "你的用户名/my-blog"
       repoId: "上一步拿到的 data-repo-id"
       category: "Announcements"
       categoryId: "上一步拿到的 data-category-id"
   ```

5. 提交并推送（见下面「日常写作流程」），等 Netlify 重新构建

读者需要登录 GitHub 账号才能评论。如果你的读者大多没有 GitHub 账号，这个功能意义不大。

---

## 日常写作流程

### 推荐：用网页后台

```
打开 https://你的网址/admin/  →  New 文章  →  写  →  Publish  →  等 30 秒
```

**手机、平板、别人的电脑上都能写**，因为后台是 Netlify 提供的网页，跟你本地装了什么完全无关。

### 本地预览（改动较大时）

```powershell
cd C:\Users\GEO\Desktop\my-blog
hugo server -D
```

浏览器打开 http://localhost:1313 ，边写边看效果，保存即刷新。

### 直接改文件（熟悉之后最快）

用任何文本编辑器改 `content/posts/` 下的 `.md` 文件，然后：

```powershell
cd C:\Users\GEO\Desktop\my-blog
git add -A
git commit -m "新增文章：xxx"
git push
```

推送后 Netlify 会自动重新构建。**你不需要手动做任何部署操作。**

---

## 已经为你做好的优化

这些不用你操心，已经配好了：

| 功能 | 说明 |
|---|---|
| **深色模式** | 跟着系统自动切换，右上角也能手动切 |
| **站内搜索** | 导航栏「搜索」，纯前端，不需要服务器 |
| **文章目录（TOC）** | 根据 `##` `###` 自动生成 |
| **阅读时长 / 字数** | 已开启，中文按字计数 |
| **代码复制按钮** | 代码块右上角，一键复制 |
| **代码高亮** | Hugo 内置 Chroma，不加载额外 JS |
| **文章归档页** | 导航栏「归档」，按年份列出全部文章 |
| **标签 / 分类页** | 自动根据 front matter 生成 |
| **RSS 订阅** | `/index.xml`，导航栏底部有图标 |
| **站点图标** | 浏览器标签页的靛蓝色小图标 |
| **中文排版优化** | 行高、字体栈、段落间距、标题间距都调过 |
| **自定义 404 页面** | 中文提示 + 返回入口 |
| **图片灯箱** | 点击正文里的图片可以全屏查看，Esc 关闭 |
| **响应式** | 手机上自动适配 |
| **SEO** | sitemap、robots.txt、OpenGraph 标签都有 |

### 想改图标颜色？

图标是用脚本生成的。打开 `tools/make-icons.mjs`，改最上面的两个颜色值：

```js
const C1 = [99, 102, 241]; // 渐变起点  #6366F1（靛蓝）
const C2 = [67, 56, 202];  // 渐变终点  #4338CA（深靛蓝）
```

然后在项目目录执行：

```powershell
node tools/make-icons.mjs
```

### 想改配色 / 排版？

打开 `assets/css/extended/custom.css` 改，这个文件里的所有内容都会自动生效，不需要在别处注册。

---

## 常见问题排查

### Netlify 构建失败，提示 HUGO_VERSION

Netlify 的构建环境可能还没收录 `0.166.0`。打开 `netlify.toml`，把版本号换成它提示中列出的可用版本，然后重新部署。

### 构建时报 `.Language.LanguageDirection deprecated` 之类的警告

**可以无视。** 这是 PaperMod 主题还没跟上 Hugo 新版 API，只是警告，不影响构建。因为 `netlify.toml` 把 Hugo 版本钉死在 `0.166.0`，这个问题不会突然恶化。

### 推送报错 `! [rejected] main -> main (fetch first)`

说明远程仓库不是空的（建仓库时误勾了 README）。执行：

```powershell
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### 后台登录不了

按顺序检查：

1. Identity 是否已 **Enable**
2. Git Gateway 是否已 **Enable**
3. 是否已经**接受邀请并设置了密码**（只注册不设密码是登录不了的）
4. Registration 是不是 **Invite only**，而你的邮箱没被邀请过
5. 浏览器按 F12 打开控制台，看看有没有红色报错

### 点了 Publish，但网站没变化

- 等 1–2 分钟，Netlify 构建需要时间
- 去 Netlify 的 **Deploys** 页面看最新部署状态。`Building` 是在跑，`Failed` 就点进去看日志
- 检查文章的**草稿**开关是不是打开了

### 首页是空的 / 文章不显示

99% 是**文章的日期在未来**。

Hugo 默认不构建"未来"的文章（`buildFuture: false`）。检查 front matter 里的 `date`：

```yaml
date: 2026-09-22T09:30:00+08:00
```

如果这个时间还没到，文章就不会出现。要么改成过去的时间，要么在 `hugo.yaml` 里把 `buildFuture` 改成 `true`。

### 图片上传了但显示不出来

- 引用路径必须是 `/images/uploads/文件名.jpg` 这种**以斜杠开头**的
- 确认文件确实提交到了 `static/images/uploads/`
- **图片文件名不要用中文和空格**，改用英文或数字

### 端口 1313 被占用

```powershell
hugo server -D --port 1314
```

### 想换主题

把 `themes/` 下的目录换成新主题，然后改 `hugo.yaml` 里的 `theme:` 字段。不同主题的 `params` 配置格式不一样，需要按新主题的文档重新配一遍。

### 以后 Git Gateway 会不会失效？

有可能。Git Gateway 目前处于「维护但不再推荐新配置」的状态。

如果哪天真的不能用了，有三条退路：

1. **换用 `github` 后端**：把 `static/admin/config.yml` 里的 `backend` 改成
   ```yaml
   backend:
     name: github
     repo: 你的用户名/my-blog
     branch: main
   ```
   然后在 GitHub 上建一个 OAuth App，并部署一个 OAuth 代理。比现在麻烦一些。

2. **换用 Sveltia CMS**：它是 Decap CMS 的兼容替代品，把 `static/admin/index.html` 里的 `./decap-cms.js` 换成 Sveltia 的文件即可，配置格式基本通用。

3. **最差情况**：直接用 GitHub 网页编辑 Markdown 文件，一样能写文章，只是没有可视化界面。

**无论哪种情况，你的文章都不会丢**——它们就是一堆 Markdown 文本，躺在你自己的 GitHub 仓库里。

---

## 备份

你其实不需要额外的备份方案：

| 内容 | 存在哪 |
|---|---|
| 文章正文 | GitHub 仓库（有完整历史记录） |
| 图片 | 同上 |
| 站点配置 | 同上 |
| 本地副本 | `C:\Users\GEO\Desktop\my-blog` |

想额外保险，定期在 GitHub 仓库页面点 **Code → Download ZIP** 下载一份存到网盘。

**没有数据库，就没有"数据库备份"这个烦恼。** 这正是静态站最大的好处之一。

---

## 总共花了多少钱

| 项目 | 费用 |
|---|---|
| GitHub 仓库 | ¥0 |
| Netlify 托管 + CDN + HTTPS | ¥0 |
| Decap CMS 后台 | ¥0 |
| Hugo | ¥0 |
| PaperMod 主题 | ¥0 |
| Giscus 评论 | ¥0 |
| 站点图标 / 排版 / 灯箱 | ¥0 |
| **合计** | **¥0** |

唯一的可选支出是自定义域名，约 ¥10–80/年，可以用支付宝购买，不需要信用卡。
